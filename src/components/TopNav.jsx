export default function TopNav() {

  return (
    // Top Nav for large screens
    <nav className="hidden md:flex items-center justify-between md:fixed top-0 w-full h-8 pl-4 bg-gray-700 text-xs z-40 sticky">
      Top Nav Desktop Only
    </nav>
  );
}